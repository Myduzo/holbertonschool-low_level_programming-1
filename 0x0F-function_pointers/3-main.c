#include "calc.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * main - function for executing arithmetic operations
 * 
 * Description: This program will print the result of an arithmetic operation
 * Return: 0 for success
 */
int main(void);
{
	int result;

	result = get_op_func(atoi[s], a, b);
	printf("%d\n", result);
	return (0);
}

